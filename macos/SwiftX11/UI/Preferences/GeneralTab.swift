import SwiftUI

struct GeneralTab: View {
    var body: some View {
        Form {
            Text("General settings for SwiftX11.")
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity, alignment: .topLeading)
    }
}
